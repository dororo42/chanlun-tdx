# DLL 使用方法

## 1. 下载 DLL

从 [Releases](https://github.com/dororo42/chanlun-tdx/releases) 页面下载对应版本的 DLL：

- `chanlun_tdx_x64.dll` — 64 位通达信使用
- `chanlun_tdx_x86.dll` — 32 位通达信使用

> **位数匹配**：通达信 32 位版使用 x86 DLL，通达信 64 位版使用 x64 DLL。位数不匹配会导致加载失败。

## 2. 安装 DLL

1. 找到通达信安装目录（通常在 `C:\new_tdx\` 或 `C:\zd_pazq\` 等）
2. 进入子目录 `T0002\dlls\`（不存在则创建）
3. 将 DLL 文件复制到此目录，重命名为 `chanlun_tdx.dll`

```
C:\new_tdx\T0002\dlls\chanlun_tdx.dll
```

## 3. 绑定 DLL

1. 打开通达信
2. 功能 → 公式系统 → 公式管理器（或按 `Ctrl+F`）
3. 左侧展开 → **DLL 函数**
4. 点击 **绑定 DLL 函数**
5. 在 **DLL 绑定1** 位置浏览选择 `T0002\dlls\chanlun_tdx.dll`
6. 点击 **确定** — 状态栏应显示绑定成功

> 绑定到 slot 1 对应公式中的 `TDXDLL1(...)` 调用。如果 slot 1 已被占用，可绑定到 slot 2/3/4，但公式中的 `TDXDLL1` 要相应改为 `TDXDLL2` 等。

## 4. 导入公式

1. 公式管理器（Ctrl+F）中点击 **新建**，按下表逐个导入：

| 顺序 | 文件 | 建议公式名 | 类型 |
|---|---|---|---|
| 1 | `formulas/缠论主图显示.txt` | `CLZT` | 技术指标公式 → 主图叠加 |
| 2 | `formulas/缠论条件选股.txt` | `CLXG` | 条件选股公式 |
| 3 | `formulas/缠论力度副图.txt` | `CLLD` | 技术指标公式 → 副图 |

三个公式**互相独立**（都直接调用 DLL，无跨公式引用），可以只导入需要的那个。

> 导入方式：新建 → 填公式名 → 选类型 → 把 .txt 内容整段粘贴到编辑区 → 确认。
> 公式里的中文注释用 `{}` 包裹，通达信会自动忽略。

## 5. 使用

### 主图显示

打开任意个股 K 线图 → 输入公式名 `CLZT` 回车 → 缠论结构叠加在主图上

### 条件选股

功能 → 选股器 → 条件选股 → 选择 `CLXG` → 执行选股

> 选股遍历全市场约需 30 秒 - 3 分钟（取决于电脑性能）。

## DLL 函数表

| Mark | 输出 | 值域 | 说明 |
|---|---|---|---|
| 1 | 分型标记 | 1 / -1 / 0 | 1=顶分型, -1=底分型, 0=无 |
| 2 | 分型价格 | float | 顶=高点, 底=低点 |
| 3 | 笔端点标记 | 1 / -1 / 0 | 1=上笔端(顶), -1=下笔端(底) |
| 4 | 笔端点价格 | float | 笔端点价格 |
| 5 | 笔中枢 ZG | float / 0 | 笔中枢上沿 |
| 6 | 笔中枢 ZD | float / 0 | 笔中枢下沿 |
| 7 | 笔中枢起止 | 1 / 0 | 中枢第一根K线=1 |
| 8 | 线段端点标记 | 1 / -1 / 0 | 1=上段端(顶), -1=下段端(底) |
| 9 | 线段端点价格 | float | 线段端点价格 |
| 10 | 线段中枢 ZG | float / 0 | 线段中枢上沿 |
| 11 | 三买 | 1 / 0 | 三类买点 |
| 12 | 三卖 | 1 / 0 | 三类卖点 |
| 13 | 二买 | 1 / 0 | 二类买点 |
| 14 | 二卖 | 1 / 0 | 二类卖点 |
| 15 | 一买 | 1 / 0 | 一类买点（趋势背驰） |
| 16 | 一卖 | 1 / 0 | 一类卖点（趋势背驰） |
| 17 | 盘整背驰 | 1 / 0 | 盘整背驰标记 |
| 18 | 线段中枢 ZD | float / 0 | 线段中枢下沿 |
| 19 | 笔中枢起止 | 1 / -1 / 0 | **v2 新增**：中枢首根=1, 末根=-1 |
| 20 | 线段中枢起止 | 1 / -1 / 0 | **v2 新增**：中枢首根=1, 末根=-1 |

> 19/20 用于让主图公式画出「上下沿轨线 + 起止竖框」的干净中枢矩形。
> 旧版实现（chan2zen/rust-chan）用 `BACKSET` 反推中枢区间，那是未来函数、历史会重绘；
> 这里由 DLL 直接给出标记，无未来函数。

## 常见问题

**DLL 加载失败** → 检查位数是否匹配（32 位通达信用 x86 DLL）

**公式报错 TDXDLL1 未定义** → DLL 未绑定或绑定到非 slot 1

**图表无显示** → DLL 加载失败或公式类型未选"主图叠加"

**主图第一笔画不出来 / 中枢被灰块糊住** → 用的是旧 DLL + 旧公式。v2.0 起 DLL 补上了首笔文端（mark 3/4），公式把实心柱改成上下沿轨线 + 起止竖框（依赖 mark 19/20）。

**中枢只有两条横线、没有起止竖框** → DLL 早于 v2.0，mark 19/20 返回 0。换成 v2.0 的 DLL 即可。

**选股结果太少** → 默认口径是「二买/三买 + 力度背驰 + 未被卖点破坏」，本身偏严。打开 `formulas/缠论条件选股.txt` 底部，换成注释里的「宽口径」行。

**选股很慢** → 正常，每只股票都要全量重算管线（毫秒级/只，全市场约 30 秒 - 3 分钟）
